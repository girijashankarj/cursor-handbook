# Command: Build

Run the project build (compile, bundle, or package).

## Instructions

1. Run the project build command (e.g. `npm run build`, `yarn build`, or `pnpm build`).
2. Use the script defined in `package.json` or project config; do not assume tool names.
3. Report build errors with file and line if available; do not run full test suite.
